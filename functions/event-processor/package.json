{
  "name": "event-processor",
  "version": "1.0.0",
  "type": "module",
  "description": "Scheduled EventBridge processor for background DynamoDB maintenance.",
  "engines": {
    "node": ">=22"
  },
  "main": "index.mjs",
  "scripts": {
    "build": "node --check src/index.mjs",
    "package": "npm run build && rm -rf dist && mkdir -p dist && zip -rq dist/event-processor.zip . -x \"dist/*\""
  },
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.515.0"
  }
}
